# 프로젝트 초기 지시문 (Claude Code용)

아래 내용을 GitHub repo 생성 후, 로컬(또는 Claude Code 연결 환경)에서 그대로 붙여넣어 시작하세요.

---

## 프로젝트 개요

1인칭 주인공 시점의 카드형 덱빌딩 던전 크롤러를 만든다. 개인 재미를 위한 솔로 개발 프로젝트이며 배포/수익화 목적은 없다.

- **장르**: 로그라이크 + 메타 성장 하이브리드 (Slay the Spire식 런 구조 + 영구 성장 요소)
- **시점/서사 톤**: 뒷골목/평범한 출신 주인공이 스킬과 장비를 카드로 습득하며 강해지는 성장 서사. 정통 판타지 톤(과도한 게임적 상태창 UI는 지양, 텍스트 연출은 몰입감 있게)
- **플랫폼**: 웹 기반 PWA (안드로이드에 홈 화면 추가로 설치), 데스크톱 브라우저도 겸용
- **참고 선례**: 동일 개발자의 이전 프로젝트 "Dawn and Twilight" (텍스트 로그라이크 PWA, React+Vite+Firebase)의 메타 성장 구조(영혼 제단, 원정/저주 시스템, 출전 준비 화면)를 이번 프로젝트에도 재해석해서 적용할 것.

## 핵심 시스템 (무한던전 자동사냥 + 가챠 구조)

### 코어 루프
1. **출전 준비**: 보유한 스킬/장비/동료 카드 중 로드아웃 구성
2. **자동 사냥**: 층(floor)이 무한 증가하는 자동전투 시뮬레이션. 스킬 쿨타임/우선순위 기반으로 전투 로직이 자동 실행되고, 로그를 애니메이션처럼 재생
3. **사망 → 환생**: 도달 층수·처치 수·획득 아이템을 기반으로 환생 포인트 정산
4. **가챠**: 환생 포인트로 스킬/장비/동료 카드 뽑기 및 중복 강화
5. **재도전**: 강화된 로스터로 로드아웃 재구성 → 1번으로 순환

### 자원 체계
- **환생 포인트**: 단일 재화. 가챠 및 경지(영구 스탯) 투자에 모두 사용
- **체력(HP)**: 0이 되면 해당 런 종료(환생)
- **정신력(Focus)**: 강력 스킬 쿨타임 자원

### 카드 카테고리
| 카테고리 | 설명 | 획득 방식 |
|---|---|---|
| 스킬 | 오러 소모, 자동전투 중 우선순위대로 발동 | 가챠 |
| 장비 | 장착형, 패시브 효과 | 가챠 |
| 동료 | 보조 딜/버프 유닛 | 가챠 |
| 경지 | 영구 스탯 트리 (뽑기 운과 무관하게 성장) | 포인트 직접 구매 |

### 환생 포인트 계산식
```
포인트 = (도달 층수 × 10) + (처치 몹 수 × 1) + (획득 아이템 등급 합산 × 5)
```

### 가챠 확률 (단일 재화, 천장 없음 — 순수 확률제)
| 등급 | 확률 |
|---|---|
| Common | 60% |
| Rare | 25% |
| Epic | 12% |
| Legendary | 3% |

중복 획득 시 해당 카드 강화 재료로 자동 전환.

### 층 난이도 스케일링
```
몹 스탯 = 기본 스탯 × (1 + 층수 × 0.08)
```

### 전투 처리 방식
실시간 방치형(틱 기반 오프라인 계산)이 아니라 **즉시 시뮬레이션 후 결과 재생** 방식으로 구현. 런 시작 시 층별 전투를 한 번에 연산해서 로그를 만들고, 그 로그를 프론트에서 애니메이션처럼 순차 재생. 서버 사이드 오프라인 시간 계산 로직이 필요 없어 구현 난이도가 훨씬 낮음.

## 기술 스택

- **프레임워크**: React + Vite, PWA 플러그인(vite-plugin-pwa)으로 설치형 앱 구성
- **스타일**: Tailwind CSS
- **애니메이션**(선택): Framer Motion — 카드 드래그/뒤집기 연출용
- **백엔드/DB**: Firebase Firestore
- **인증**: Firebase Anonymous Auth (로그인 UI 없이 기기 식별용, 추후 필요 시 기기 간 연동 고려)
- **배포**: GitHub Pages + GitHub Actions 자동 빌드/배포 (푸시 시 자동 배포되도록 워크플로우 구성)

## 파일 구조 (초기 스캐폴딩)

```
/src
  /components   → 카드, 맵 노드, 전투 UI 등 프레젠테이션 컴포넌트
  /systems      → 전투 로직, 덱 관리, 자원 계산 등 순수 로직
  data.js       → 카드/적/이벤트 정의 (스킬/장비/동료/경지 스키마)
  firebase.js   → Firestore 초기화 및 저장/불러오기 함수
  App.jsx       → 화면 라우팅 및 전역 상태
index.html
vite.config.js  → PWA 매니페스트 포함
package.json
.github/workflows/deploy.yml  → GitHub Pages 자동 배포
```

## Claude Code에게 요청할 첫 작업

1. 위 파일 구조로 Vite + React + Tailwind 프로젝트 스캐폴딩 생성
2. `vite-plugin-pwa`로 PWA 매니페스트 설정 (앱 이름, 아이콘 placeholder, 안드로이드 설치 가능하도록)
3. `firebase.js`에 Firestore 초기화 코드 작성 (config는 환경변수 `.env`로 분리, `.env.example` 함께 생성)
4. `data.js`에 카드 스키마 정의 (아래 형태 참고해서 스타터 카드 5~10장 샘플 포함, 스킬/장비/동료 3종 골고루)
   ```js
   {
     id: "skill_001",
     name: "기본 검격",
     category: "skill", // skill | equipment | companion
     rarity: "common",   // common | rare | epic | legendary
     cost: 1,             // 오러 소모 (스킬만 해당)
     cooldown: 2,          // 자동전투 시 재사용 대기 (초 or 턴 단위)
     priority: 1,           // 낮을수록 먼저 발동
     effect: { type: "damage", value: 6 },
     description: "적에게 6의 피해를 입힌다."
   }
   ```
5. `systems/battleSimulator.js` — 로드아웃(스킬/장비/동료 배열)과 층수를 입력받아 전투 로그(JSON 배열)를 반환하는 순수 함수 구현. 층별 몬스터 스탯은 `몹 스탯 = 기본 스탯 × (1 + 층수 × 0.08)` 공식 적용
6. `systems/gacha.js` — 환생 포인트 소모 → 등급 확률(common 60/rare 25/epic 12/legendary 3)에 따라 카드 1장 반환, 중복 시 강화 재료 처리 로직
7. 기본 화면 3개까지 최소 동작 프로토타입: (1) 출전 준비(로드아웃 구성) (2) 자동전투 로그 재생 화면 (3) 가챠 화면
8. `.github/workflows/deploy.yml` 작성 — main 브랜치 push 시 build → GitHub Pages 배포
9. README.md에 로컬 실행 방법 및 Firebase 프로젝트 연결 방법 간단히 정리

## 주의사항

- 특정 웹툰의 고유명사(등장인물 이름, 세계관 고유 용어)를 그대로 쓰지 말고 오마주 수준으로 재해석한 자체 명칭 사용 (Dawn and Twilight 때와 동일한 방식)
- Firestore 보안 규칙은 익명 인증 사용자 본인 데이터만 read/write 가능하도록 최소 설정
- 모바일 화면(세로 비율) 우선 UI로 설계
